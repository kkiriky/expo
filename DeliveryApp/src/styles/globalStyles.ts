import {StyleSheet} from 'react-native';

const globalStyles = StyleSheet.create({
  full: {
    flex: 1,
  },
  row: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});

export default globalStyles;
